{
  "name": "nezhos",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "release": "turbo run release",
    "clean": "npx rimraf ./**/node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "nezhos": "nezhos"
  },
  "devDependencies": {
    "@changesets/cli": "2.25.0",
    "@nezhos/tools": "workspace:*",
    "prettier": "latest",
    "turbo": "latest"
  },
  "engines": {
    "npm": ">=8.0.0",
    "node": ">=14"
  },
  "packageManager": "pnpm@7.14.0",
  "volta": {
    "node": "18.12.0"
  },
  "lint-staged": {
    "**/*.{js,ts,jsx,tsx}": [
      "eslint --fix"
    ]
  }
}